apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: food-delivery
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/version: "1.0.0"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003
